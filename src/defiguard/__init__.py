"""DeFiGuard artifact implementation (CCS submission bundle).

This repository provides:
- A minimal, deterministic reproduction path that regenerates all paper tables/figures from included raw logs.
- A toy, self-contained protocol simulator + multi-agent search loop that mirrors the paper's algorithmic structure.

Safety note: This artifact does not ship weaponized exploit bundles for live targets.
"""

__all__ = ["__version__"]
__version__ = "0.1.0"
